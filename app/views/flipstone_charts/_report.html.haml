- chart_type ||= 'ColumnChart'
.report{'data-chart-type' => chart_type}
  %table
    %caption= report.title
    %thead
      %tr
        %th
        - report.x_axis_labels.each do |l|
          %th= l
    %tbody
      - report.data.each_with_index do |row, i|
        %tr
          %th= report.y_axis_labels[i]
          - row.each do |cell|
            %td= cell

